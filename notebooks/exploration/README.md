# Exploratory notebooks

Notebooks in this directory are for **EDA only** and must stay separate from
the reproducible training pipeline (see `Docs/financial_time_series_ml_system_roadmap.docx`,
Phase 2). Anything used for training, evaluation or serving belongs in
`src/financial_ml/` and is exercised by tests.
