# data/

Subdirectories:

- `raw/` — immutable market-data downloads from ingestion (Phase 1).
- `processed/` — validated, canonical OHLCV tables.
- `features/` — feature-engineered datasets.

These directories are git-ignored; they are populated by the pipeline.
